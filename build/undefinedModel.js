"use strict";
class undefined {
    constructor(UserId, Name, Username, Email, PasswordHash, Salt) { this.UserId = UserId; this.Name = Name; this.Username = Username; this.Email = Email; this.PasswordHash = PasswordHash; this.Salt = Salt; }
}
